{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "npm run build-calendar",
        "build": "vite build",
        "build-calendar": "cd nova-components/Calendar && npm run watch",
        "build-calendar-prod": "cd nova-components/Calendar && npm run prod"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.5",
        "vite": "^4.0.0"
    }
}
